@model IEnumerable<business.Pagina>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Servico.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodigoHtml)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Servico.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoHtml)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPagina }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPagina }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPagina })
        </td>
    </tr>
}

</table>
